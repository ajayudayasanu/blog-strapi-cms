runtime: nodejs22
instance_class: F2

automatic_scaling:
  min_instances: 1
  max_instances: 1
  target_cpu_utilization: 0.65

env_variables:
  HOST: '0.0.0.0'
  PORT: '8080'
  NODE_ENV: 'production'

  # Strapi secrets — replace placeholders with real values (never commit app.yaml)
  APP_KEYS: '__APP_KEYS__'
  API_TOKEN_SALT: '__API_TOKEN_SALT__'
  ADMIN_JWT_SECRET: '__ADMIN_JWT_SECRET__'
  TRANSFER_TOKEN_SALT: '__TRANSFER_TOKEN_SALT__'
  ENCRYPTION_KEY: '__ENCRYPTION_KEY__'
  JWT_SECRET: '__JWT_SECRET__'

  # Database (Private IP via VPC connector)
  DATABASE_CLIENT: 'postgres'
  DATABASE_NAME: 'postgres'
  DATABASE_USERNAME: 'postgres'
  DATABASE_PASSWORD: '__DATABASE_PASSWORD__'
  DATABASE_HOST: '__DATABASE_HOST__'
  DATABASE_PORT: '5432'
  DATABASE_SSL: 'false'

  # GCS (Google Cloud Storage)
  GCS_BUCKET_NAME: 'strapi-cms-storage-bucket'
  GCS_BASE_PATH: 'cms'

vpc_access_connector:
  name: projects/strapi-cms-505607/locations/us-central1/connectors/strapi-connector

# beta_settings removed — not needed for Private IP connections

handlers:
  - url: /.*
    script: auto
    secure: always
